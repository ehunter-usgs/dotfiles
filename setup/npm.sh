#!/usr/bin/env bash

# As of Yeoman 1.0, this gets bower and grunt-cli as well
npm install -g yo

npm install -g jshint
npm install -g jake

npm install -g generator-webapp
npm install -g generator-generator
npm install -g generator-hazdev-webapp
npm install -g generator-hazdev-weblib

exit 0;
